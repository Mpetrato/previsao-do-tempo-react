import styled from "styled-components";

export const Container = styled.div`
    padding: 40px;

    & > h1 {
        font-size: 24px;
        font-weight: bold;
        color: white;
        text-transform: uppercase;
    }
`